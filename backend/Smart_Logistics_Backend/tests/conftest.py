import os
import sys

# Ensure the package root (the Smart_Logistics_Backend folder) is on sys.path
ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), '..'))
if ROOT not in sys.path:
    sys.path.insert(0, ROOT)
