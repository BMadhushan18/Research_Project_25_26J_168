This folder will contain Jupyter notebooks demonstrating training runs and evaluations.

Run `backend/Smart_Logistics_Backend/scripts/generate_synthetic_data.py` and `backend/Smart_Logistics_Backend/scripts/train_ml.py` before opening the notebooks to ensure artifacts exist.

A full `train_ml.ipynb` can be added on request.